datasource db {
    provider = "sqlite"
    url      = "file:./primes.sqlite"
}

generator client {
    provider = "prisma-client"
    output   = "prisma-db"
    runtime  = "deno"
    library  = "client"
}

model consecutive {
    id Int @id
}

model alone {
    id Int @id
}
