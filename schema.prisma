datasource db {
    provider = "sqlite"
    url      = "file:./primes.sqlite?query_timeout=21000"
}

generator client {
    provider = "prisma-client"
    output   = "prisma-db"
    runtime  = "deno"
}

model consecutive {
    id Int @id
}

model alone {
    id Int @id
}
